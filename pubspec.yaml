name: food_commerce_v2
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  # State Management
  flutter_bloc: ^8.1.0
  equatable: ^2.0.5 # For value comparisons in BLoC state

  # Architecture & DI
  get_it: ^7.6.0 # Dependency Injection
  dartz: ^0.10.1 # Functional programming (Either<Failure, Success>)

  # Backend
  supabase_flutter: ^2.0.0

  # Navigation & UI
  go_router: ^12.0.0 # Better for deep linking/web support
  google_fonts: ^6.1.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  #
